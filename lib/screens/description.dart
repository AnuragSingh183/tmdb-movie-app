import 'package:flutter/material.dart';

class Description class  extends StatelessWidget {
  final String name,description,posterurl,rating,release date;
  

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}